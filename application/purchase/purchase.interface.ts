import { ServiceInterface } from "../service.interface";

export interface PurchaseInterfaceService<TEntityId, TEntityQuery, TEntity> extends 
    ServiceInterface<TEntityId, TEntityQuery, TEntity>
{}